{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "fastapi",
  "buildCommand": "npm --prefix frontend ci --include=dev && npm --prefix frontend run build && test -f frontend/dist/index.html",
  "functions": {
    "api/index.py": {
      "maxDuration": 180
    }
  }
}
