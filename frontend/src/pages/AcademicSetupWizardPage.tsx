import { useEffect, useState } from 'react'
import { PageHeader } from '../components/PageHeader'
import { Alert } from '../components/Alert'
import { Badge } from '../components/States'
import { useToast } from '../components/Toast'
import { api, friendlyApiError, type AcademicYear, type Level } from '../lib/api'
import { scheduling, type SchoolClass } from '../lib/scheduling'

const steps = ['Academic Year', 'Level', 'Class', 'Class Code']
type FormState = { yearId:string; yearName:string; startDate:string; endDate:string; levelId:string; className:string; classCode:string }
const initialForm:FormState={yearId:'',yearName:'',startDate:'',endDate:'',levelId:'',className:'',classCode:''}
const allowedLevels = ['Pre Primary','Primary School','Junior School','Senior School']

export function AcademicSetupWizardPage(){
 const {notify}=useToast(); const [step,setStep]=useState(0); const [loading,setLoading]=useState(true); const [saving,setSaving]=useState(false); const [error,setError]=useState<string|null>(null); const [years,setYears]=useState<AcademicYear[]>([]); const [levels,setLevels]=useState<Level[]>([]); const [classes,setClasses]=useState<SchoolClass[]>([]); const [form,setForm]=useState<FormState>(initialForm)
 async function load(){setLoading(true);setError(null);try{const [y,l,c]=await Promise.all([api.academicYears(),api.levels(),scheduling.classes()]);setYears(y);setLevels(l);setClasses(c)}catch(e){setError(friendlyApiError(e,'load academic setup data'))}finally{setLoading(false)}}
 useEffect(()=>{void load()},[])
 function selectYear(id:string){const y=years.find(x=>String(x.id)===id);setForm(v=>y?{...v,yearId:String(y.id),yearName:y.name,startDate:y.start_date,endDate:y.end_date}:{...v,yearId:'',yearName:'',startDate:'',endDate:''})}
 function selectLevel(id:string){const l=levels.find(x=>String(x.id)===id);setForm(v=>({...v,levelId:l?String(l.id):'',className:'',classCode:''}))}
 function validate(){if(step===0&&(!form.yearId&&(!form.yearName.trim()||!form.startDate||!form.endDate)))throw Error('Select an academic year or enter the year and its dates.');if(step===1&&!form.levelId)throw Error('Select a level.');if(step===2&&!form.className.trim())throw Error('Enter the class name, for example Grade 5R.');if(step===3&&!form.classCode.trim())throw Error('Enter the class code, for example 5R.')}
 async function saveYear(){if(form.yearId)return;const y=await api.createAcademicYear({name:form.yearName.trim(),start_date:form.startDate,end_date:form.endDate,is_current:false,status:'ACTIVE'});setYears(v=>[...v,y]);setForm(v=>({...v,yearId:String(y.id)}))}
 async function finish(){setSaving(true);setError(null);try{await saveYear();const yearId=Number(form.yearId||years.find(y=>y.name===form.yearName.trim())?.id);if(!yearId)throw Error('The academic year could not be resolved.');const levelId=Number(form.levelId);const className=form.className.trim();const classCode=form.classCode.trim().toUpperCase();const existing=classes.find(c=>String(c.code||'').toUpperCase()===classCode);if(!existing){const created=await scheduling.createClass({name:className,code:classCode,level_id:levelId,academic_year_id:yearId});setClasses(v=>[...v,created])}notify('Academic setup saved.','success');setStep(0);setForm(initialForm)}catch(e){setError(friendlyApiError(e,'save academic setup'))}finally{setSaving(false)}}
 async function next(){if(saving)return;setError(null);try{validate();if(step<steps.length-1){if(step===0)await saveYear();setStep(v=>v+1)}else await finish()}catch(e){setError(friendlyApiError(e,'continue academic setup'))}}
 const selectedYear=years.find(y=>String(y.id)===form.yearId); const selectedLevel=levels.find(l=>String(l.id)===form.levelId)
 const levelName=(levelId?:number|null)=>levels.find(l=>Number(l.id)===Number(levelId))?.name||'—'
 const yearName=(yearId?:number|null)=>years.find(y=>Number(y.id)===Number(yearId))?.name||'—'
 return <><PageHeader title="Academic setup" description="Academic Year → Level → Class → Class Code. Class is the only class entity administrators need to manage." breadcrumbs={[{label:'Dashboard',to:'/'},{label:'Academic setup'}]}/>{error&&<Alert tone="error" title="Setup could not continue">{error}</Alert>}<section className="card section"><div className="chip-list" aria-label="Setup progress">{steps.map((label,i)=><Badge key={label} tone={i===step?'success':undefined}>{i+1}. {label}</Badge>)}</div>{loading?<p>Loading setup options…</p>:<form className="form" onSubmit={e=>{e.preventDefault();void next()}}><h2 className="section__title">Step {step+1}: {steps[step]}</h2>{step===0&&<><label className="label">Academic year</label><select className="input" value={form.yearId} onChange={e=>selectYear(e.target.value)}><option value="">Create new…</option>{years.map(y=><option key={y.id} value={y.id}>{y.name}</option>)}</select>{!form.yearId&&<div className="form form--grid"><input className="input" placeholder="2026/2027" value={form.yearName} onChange={e=>setForm(v=>({...v,yearName:e.target.value}))}/><input className="input" type="date" value={form.startDate} onChange={e=>setForm(v=>({...v,startDate:e.target.value}))}/><input className="input" type="date" value={form.endDate} onChange={e=>setForm(v=>({...v,endDate:e.target.value}))}/></div>}</>}{step===1&&<><label className="label">Level</label><select className="input" value={form.levelId} onChange={e=>selectLevel(e.target.value)}><option value="">Select level…</option>{levels.filter(l=>allowedLevels.includes(l.name)).map(l=><option key={l.id} value={l.id}>{l.name}</option>)}</select><small>Pre Primary · Primary School · Junior School · Senior School</small></>}{step===2&&<><label className="label">Class</label><input className="input" placeholder="Grade 5R" value={form.className} onChange={e=>setForm(v=>({...v,className:e.target.value}))}/><small>The class name staff and students will see.</small></>}{step===3&&<><label className="label">Class code</label><input className="input" placeholder="5R" value={form.classCode} onChange={e=>setForm(v=>({...v,classCode:e.target.value.toUpperCase()}))}/><small>This is the stable business identifier used by timetabling, examinations, student enrollment and finance.</small><div className="card" style={{padding:'1rem'}}><p><strong>Academic year:</strong> {selectedYear?.name||form.yearName}</p><p><strong>Level:</strong> {selectedLevel?.name}</p><p><strong>Class:</strong> {form.className}</p><p><strong>Class code:</strong> {form.classCode.toUpperCase()}</p></div></>}<div className="form__row"><button type="button" className="button button--ghost" disabled={step===0||saving} onClick={()=>setStep(v=>Math.max(0,v-1))}>Previous</button><button className="button button--primary" disabled={saving}>{saving?'Saving…':step===steps.length-1?'Save Academic Setup':'Save & Next'}</button></div></form>}</section><section className="card section" aria-labelledby="created-academic-structure"><div className="section__header"><div><h2 id="created-academic-structure" className="section__title">Registered academic structure</h2><p>All levels and classes currently created for the school, grouped by academic year.</p></div><button type="button" className="button button--ghost button--sm" onClick={()=>void load()} disabled={loading}>Refresh</button></div>{loading?<p>Loading registered academic structure…</p>:<div className="table-wrap"><table className="table"><thead><tr><th scope="col">Academic year</th><th scope="col">Level</th><th scope="col">Class</th><th scope="col">Class code</th></tr></thead><tbody>{classes.length===0?<tr><td colSpan={4}>No classes have been created yet.</td></tr>:classes.map(c=><tr key={c.id}><td>{yearName(c.academic_year_id)}</td><td>{levelName(c.level_id)}</td><td><strong>{c.name}</strong></td><td><Badge tone="success">{c.code}</Badge></td></tr>)}</tbody></table></div>}</section></>}
