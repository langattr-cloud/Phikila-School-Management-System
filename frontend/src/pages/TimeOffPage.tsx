import { useEffect, useMemo, useState } from 'react'
import { PageHeader } from '../components/PageHeader'
import { Alert } from '../components/Alert'
import { useToast } from '../components/Toast'
import { friendlyApiError } from '../lib/api'
import { scheduling, type Calendar, type Constraint, type SchoolClass, type Slots, type Subject, type Teacher } from '../lib/scheduling'

const EMPTY: Slots = {}
type Kind = 'teachers' | 'classes' | 'subjects'
type Resource = Teacher | SchoolClass | Subject
type CellState = 'empty' | 'available' | 'blocked'

function key(day: number, period: number) { return `${day}:${period}` }
function toStates(slots: Slots): Record<string, CellState> { const out: Record<string, CellState> = {}; Object.entries(slots).forEach(([d, ps]) => (ps ?? []).forEach(p => { out[key(Number(d), p)] = 'blocked' })); return out }
function toSlots(states: Record<string, CellState>): Slots { const out: Slots = {}; Object.entries(states).forEach(([k, state]) => { if (state !== 'blocked') return; const [d, p] = k.split(':').map(Number); (out[String(d)] ??= []).push(p) }); Object.values(out).forEach(a => a.sort((a, b) => a - b)); return out }
function subjectSlots(rows: Constraint[], id: number): Slots { const row = rows.find(r => r.kind === 'avoid_lessons' && r.scope === 'subject' && r.target_id === id && r.enabled); const slots = row?.params?.slots; return slots && typeof slots === 'object' ? slots as Slots : EMPTY }

export function TimeOffPage() {
  const { notify } = useToast()
  const [kind, setKind] = useState<Kind>('teachers'), [teachers, setTeachers] = useState<Teacher[]>([]), [classes, setClasses] = useState<SchoolClass[]>([]), [subjects, setSubjects] = useState<Subject[]>([]), [constraints, setConstraints] = useState<Constraint[]>([]), [calendar, setCalendar] = useState<Calendar | null>(null), [selectedId, setSelectedId] = useState<number | null>(null), [states, setStates] = useState<Record<string, CellState>>({}), [saved, setSaved] = useState<Record<string, CellState>>({}), [loading, setLoading] = useState(true), [saving, setSaving] = useState(false), [error, setError] = useState<string | null>(null)
  const resources = useMemo<Resource[]>(() => kind === 'teachers' ? teachers : kind === 'classes' ? classes : subjects, [kind, teachers, classes, subjects])
  const selected = resources.find(r => r.id === selectedId) ?? null, days = calendar?.days.filter(d => d.is_active) ?? [], periods = calendar?.periods.filter(p => p.is_teaching) ?? [], dirty = JSON.stringify(states) !== JSON.stringify(saved), label = kind === 'teachers' ? 'Teacher' : kind === 'classes' ? 'Class' : 'Learning area'
  useEffect(() => { let live = true; Promise.allSettled([scheduling.calendar(), scheduling.teachers(), scheduling.classes(), scheduling.subjects(), scheduling.constraints()]).then(r => { if (!live) return; const [c, t, cl, s, co] = r; if (c.status === 'fulfilled') setCalendar(c.value); if (t.status === 'fulfilled') setTeachers(t.value); if (cl.status === 'fulfilled') setClasses(cl.value); if (s.status === 'fulfilled') setSubjects(s.value); if (co.status === 'fulfilled') setConstraints(co.value); if (t.status === 'fulfilled') setSelectedId(t.value[0]?.id ?? null); const f = r.find(x => x.status === 'rejected'); if (f?.status === 'rejected') setError(friendlyApiError(f.reason, 'load the time-off grid')) }).finally(() => live && setLoading(false)); return () => { live = false } }, [])
  useEffect(() => { if (!selected) { setStates({}); setSaved({}); return }; const slots = kind === 'subjects' ? subjectSlots(constraints, selected.id) : (selected as Teacher | SchoolClass).unavailable ?? EMPTY; const next = toStates(slots); setStates(next); setSaved(next) }, [selected, kind, constraints])
  useEffect(() => { if (resources.length && !resources.some(r => r.id === selectedId)) setSelectedId(resources[0].id) }, [resources, selectedId])
  function cycle(day: number, period: number) { const k = key(day, period); setStates(s => ({ ...s, [k]: s[k] === 'empty' ? 'available' : s[k] === 'available' ? 'blocked' : 'empty' })) }
  async function save() { if (!selected || saving || !dirty) return; setSaving(true); try { const draft = toSlots(states); if (kind === 'teachers') { const u = await scheduling.updateTeacher(selected.id, { ...selected, unavailable: draft }); setTeachers(x => x.map(i => i.id === u.id ? u : i)) } else if (kind === 'classes') { const u = await scheduling.updateClass(selected.id, { ...selected, unavailable: draft }); setClasses(x => x.map(i => i.id === u.id ? u : i)) } else { const old = constraints.filter(i => i.kind === 'avoid_lessons' && i.scope === 'subject' && i.target_id === selected.id); for (const row of old) await scheduling.deleteConstraint(row.id); if (Object.keys(draft).length) { const created = await scheduling.createConstraint({ kind: 'avoid_lessons', scope: 'subject', target_id: selected.id, is_hard: true, weight: 100, params: { slots: draft }, enabled: true, note: `${selected.name} time off` }); setConstraints(x => [...x.filter(i => !old.some(o => o.id === i.id)), created]) } } setSaved(states); notify(`${selected.name} time off saved.`, 'success') } catch (e) { setError(friendlyApiError(e, 'save time-off changes')) } finally { setSaving(false) } }
  if (loading) return <><PageHeader title="Time off" description="Mark unavailable lesson periods using the timetable grid." breadcrumbs={[{ label: 'Dashboard', to: '/' }, { label: 'Scheduling' }, { label: 'Time off' }]}/><section className="card section"><p>Loading time-off grid…</p></section></>
  return <><PageHeader title="Time off" description="Set precise availability directly on the timetable grid." breadcrumbs={[{ label: 'Dashboard', to: '/' }, { label: 'Scheduling' }, { label: 'Time off' }]} actions={<button className="button button--primary button--sm" onClick={save} disabled={!dirty || saving}>{saving ? 'Saving…' : 'Save changes'}</button>}/>{error && <Alert tone="error">{error}</Alert>}<section className="card section timeoff-panel"><div className="timeoff-picker"><div><span className="field__label">Choose what you are scheduling</span><div className="timeoff-type-buttons">{([['teachers','Teachers'],['classes','Classes'],['subjects','Subjects']] as const).map(([v,l]) => <button key={v} type="button" className={`timeoff-type-button${kind === v ? ' timeoff-type-button--active' : ''}`} onClick={() => { if (!dirty || window.confirm('Discard unsaved changes?')) { setKind(v); setSelectedId(null) } }}>{l}</button>)}</div></div><label className="field timeoff-resource"><span className="field__label">Select {label.toLowerCase()}</span><select className="input" value={selectedId ?? ''} onChange={e => setSelectedId(Number(e.target.value))}>{resources.map(r => <option key={r.id} value={r.id}>{r.name}{r.code ? ` (${r.code})` : ''}</option>)}</select></label></div><div className="timeoff-toolbar"><span>Empty → <b className="green">✓</b> → <b className="red">X</b> → Empty</span><span>{dirty ? 'Unsaved changes' : 'All changes saved'}</span></div>{selected && days.length && periods.length ? <><div className="timeoff-grid-shell"><table className="timeoff-grid"><thead><tr><th>DAY</th>{periods.map((p,i) => <th key={p.index}>P{i+1}</th>)}</tr></thead><tbody>{days.map(day => <tr key={day.index}><th><strong>{day.name}</strong></th>{periods.map(period => { const state = states[key(day.index, period.index)] ?? 'empty'; return <td key={period.index} className="timeoff-grid__cell"><button type="button" className="timeoff-grid__button" onClick={() => cycle(day.index, period.index)} aria-label={`${day.name}, ${period.name}: ${state}`}>{state === 'available' && <svg className="status-icon green-icon" viewBox="0 0 32 32"><path d="M6 17l6 6L26 8"/></svg>}{state === 'blocked' && <svg className="status-icon red-icon" viewBox="0 0 32 32"><path d="M7 7l18 18M25 7L7 25"/></svg>}</button></td>})}</tr>)}</tbody></table></div><div className="timeoff-actions"><button className="button button--ghost button--sm" onClick={() => setStates({})} disabled={!Object.keys(states).length}>Clear all</button><button className="button button--primary" onClick={save} disabled={!dirty || saving}>{saving ? 'Saving…' : 'Save time off'}</button></div></> : <div className="timeoff-empty">No timetable periods available.</div>}</section><style>{`.timeoff-panel{overflow:hidden}.timeoff-picker{display:flex;gap:1rem;align-items:end;padding:1rem}.timeoff-type-buttons{display:flex;gap:.5rem;margin-top:.35rem}.timeoff-type-button{padding:.55rem .85rem;border:1px solid #dbe3ec;background:#fff;border-radius:7px}.timeoff-type-button--active{border-color:#2563eb;background:#eff6ff}.timeoff-resource{min-width:240px}.timeoff-toolbar{display:flex;justify-content:space-between;padding:.7rem 1rem;border-block:1px solid #e2e8f0}.green{color:#16a34a}.red{color:#dc2626}.timeoff-grid{width:100%;border-collapse:separate;border-spacing:0}.timeoff-grid th,.timeoff-grid td{border-right:1px solid #e2e8f0;border-bottom:1px solid #e2e8f0}.timeoff-grid th{padding:.65rem;background:#f8fafc}.timeoff-grid__cell{padding:.3rem;background:#fff}.timeoff-grid__button{width:100%;height:3.1rem;border:1px solid #e2e8f0;border-radius:6px;background:#fff;display:flex;align-items:center;justify-content:center;box-shadow:none;cursor:pointer}.timeoff-grid__button:hover{border-color:#94a3b8;box-shadow:none}.status-icon{width:2rem;height:2rem;fill:none;stroke-width:4.5;stroke-linecap:round;stroke-linejoin:round}.green-icon{stroke:#16a34a}.red-icon{stroke:#dc2626}.timeoff-actions{display:flex;justify-content:space-between;padding:1rem}`}</style></section></>}
