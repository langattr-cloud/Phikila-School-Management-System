# All VITE_* values are bundled into the browser. Use only the publishable/anon key.
VITE_SUPABASE_URL=https://PROJECT_REF.supabase.co
VITE_SUPABASE_ANON_KEY=your-supabase-publishable-or-anon-key
# Optional. Omit for same-origin (FastAPI serves this frontend on the same
# domain). Set only if the frontend is hosted on a different origin.
# VITE_API_URL=https://your-backend.vercel.app
